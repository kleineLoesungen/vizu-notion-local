---
phase: 03-user-experience
plan: 06
type: execute
wave: 4
depends_on:
  - "03-04"
files_modified:
  - pages/visualizations/[sourceId].vue
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "A source selector is visible in the viz page header"
    - "The selector lists all configured sources by name"
    - "Selecting a different source navigates to /visualizations/{selectedSourceId}"
    - "The currently active source is shown as the selected/default option"
  artifacts:
    - path: "pages/visualizations/[sourceId].vue"
      provides: "Source selector dropdown in page header"
      contains: "useFetch('/api/sources'"
  key_links:
    - from: "pages/visualizations/[sourceId].vue"
      to: "/api/sources"
      via: "useFetch('/api/sources') to get source list for dropdown options"
      pattern: "useFetch.*api/sources"
    - from: "source selector dropdown"
      to: "navigateTo('/visualizations/{id}')"
      via: "@change handler on <select> element"
      pattern: "navigateTo.*visualizations"
---

<objective>
Close Gap 2 and the source-selector portion of Gap 4: add a source selector dropdown to the visualization page header so users can switch between configured sources without returning to the dashboard.

Purpose: Currently, switching sources requires navigating back to the dashboard and clicking a different card — a multi-step detour. A dropdown in the viz page header makes source switching a single action.
Output: Source selector `<select>` dropdown in the viz page header, fetching the full source list from `/api/sources`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-user-experience/03-04-SUMMARY.md

<interfaces>
From server/routes/api/sources.get.ts — response shape:
```typescript
{
  sources: Array<{
    id: string           // same as databaseId
    name: string         // human-readable source name
    databaseId: string
    columnMappings: Record<string, string>
  }>
}
```

From pages/visualizations/[sourceId].vue — existing script setup:
```typescript
const route = useRoute()
const sourceId = computed(() => route.params.sourceId as string)

const { pages, columnMappings, sourceName, isLoading, fetchError, isMetroEligible, isFlowEligible }
  = useSourceData(sourceId)
```

The `sourceName` computed gives the current source's display name.
`activeVizType` ref holds the current viz type ('metro' | 'flow').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source selector to viz page header</name>
  <files>pages/visualizations/[sourceId].vue</files>
  <read_first>
    - pages/visualizations/[sourceId].vue (MUST read — modifying header section and script setup)
    - server/routes/api/sources.get.ts (confirm response shape: sources[].id, sources[].name)
  </read_first>
  <action>
Add a source selector `<select>` dropdown to the viz page header. Users select a source from the dropdown and are navigated to `/visualizations/{sourceId}` for that source, preserving the active viz type in the URL.

**Changes to `<script setup>`:**

Add a `useFetch` call for the sources list and a `handleSourceChange` function. Insert immediately after the existing imports (before the `const route = useRoute()` line):

No new imports needed — `useFetch` and `navigateTo` are Nuxt auto-imports.

Add after the existing `useSourceData(...)` call block (after `isFlowEligible` is destructured):

```typescript
// Gap 2/4 fix: source selector — fetch all sources for the dropdown
const { data: sourcesData } = useFetch('/api/sources')
const allSources = computed(() => sourcesData.value?.sources ?? [])

// Navigate to a different source when user selects from dropdown
const handleSourceChange = (event: Event) => {
  const selectedId = (event.target as HTMLSelectElement).value
  if (selectedId && selectedId !== sourceId.value) {
    navigateTo(`/visualizations/${selectedId}?vizType=${activeVizType.value}`)
  }
}
```

**Changes to the template:**

Find the page header block (the `<div class="p-6 pb-0">` section). It currently contains:
1. `← Back to sources` link
2. `<div class="flex items-center justify-between">` with the page title and action buttons

Replace the heading row to include a source selector. Change the `<h1>` to show just the source name and add a `<select>` element next to it:

```html
<!-- Page header -->
<div class="p-6 pb-0">
  <NuxtLink to="/" class="text-sm text-blue-600 hover:underline mb-2 block">
    ← Back to sources
  </NuxtLink>
  <div class="flex items-center justify-between gap-4">
    <!-- Source selector (Gap 2/4 fix) -->
    <div class="flex items-center gap-3 min-w-0">
      <span class="text-sm text-gray-500 flex-shrink-0">Source:</span>
      <select
        v-if="allSources.length > 0"
        :value="sourceId"
        class="text-xl font-semibold text-gray-900 bg-transparent border-none outline-none cursor-pointer hover:text-blue-600 min-w-0 max-w-xs truncate"
        :aria-label="'Switch source — currently viewing ' + sourceName"
        @change="handleSourceChange"
      >
        <option
          v-for="src in allSources"
          :key="src.id"
          :value="src.id"
        >{{ src.name }}</option>
      </select>
      <!-- Fallback while sources are loading -->
      <h1 v-else class="text-xl font-semibold text-gray-900">
        {{ isLoading ? 'Loading...' : sourceName }}
      </h1>
    </div>
    <!-- Export + Copy Link buttons (shown when viz is loaded) -->
    <div v-if="!isLoading && !fetchError && filteredPages.length > 0" class="flex items-center gap-3 flex-shrink-0">
      <ExportButton
        :viz-type="activeVizType"
        :container-id="activeVizType === 'metro' ? metrovizContainerId : FLOW_CONTAINER_ID"
      />
      <button
        @click="handleCopyLink"
        class="px-4 py-2 rounded text-sm font-medium bg-gray-100 text-gray-700 hover:bg-gray-200"
      >
        {{ copyLinkSuccess === null ? 'Copy Link' : copyLinkSuccess ? 'Copied!' : 'Copy failed' }}
      </button>
    </div>
  </div>
</div>
```

The `<h1>` tag that previously showed `{{ isLoading ? 'Loading...' : 'Visualization: ${sourceName}' }}` is replaced by the `<select>` element. The select element is styled with `bg-transparent border-none` so it reads as a heading but is still interactive. The currently active source is pre-selected via `:value="sourceId"`.

No changes to the rest of the template or the other `<script setup>` sections.
  </action>
  <verify>
    <automated>grep -n "handleSourceChange\|allSources\|sourcesData\|useFetch.*api/sources\|Switch source" /Users/sebastianwiller/Documents/github/vizu-notion-local/pages/visualizations/\[sourceId\].vue</automated>
  </verify>
  <acceptance_criteria>
    - `pages/visualizations/[sourceId].vue` contains `useFetch('/api/sources')`
    - File contains `allSources` computed ref
    - File contains `handleSourceChange` function
    - File contains `navigateTo` call inside `handleSourceChange` pointing to `/visualizations/${selectedId}`
    - File contains `@change="handleSourceChange"` on the `<select>` element
    - File contains `:value="sourceId"` on the `<select>` element (pre-selects current source)
    - File contains `v-for="src in allSources"` for rendering source options
    - File contains `'Switch source — currently viewing '` aria-label
    - File still contains `← Back to sources` back nav link (not removed)
    - File still contains `handleCopyLink`, `handleFlowNodeClick`, `handleMetroNodeClick` (existing functionality preserved)
  </acceptance_criteria>
  <done>A source selector dropdown appears in the viz page header. It lists all configured sources from /api/sources. The currently viewed source is pre-selected. Choosing a different source navigates to that source's viz page, preserving the active viz type. The "Back to sources" link is preserved as a secondary navigation option.</done>
</task>

</tasks>

<verification>
- `grep "useFetch.*api/sources" pages/visualizations/\[sourceId\].vue` exits 0
- `grep "handleSourceChange" pages/visualizations/\[sourceId\].vue` exits 0
- `grep "navigateTo.*visualizations" pages/visualizations/\[sourceId\].vue` exits 0
</verification>

<success_criteria>
- Source selector dropdown is visible in the viz page header on every source page
- Dropdown lists all configured sources by name
- Currently active source is pre-selected in the dropdown
- Selecting a different source navigates to that source's visualization, preserving viz type
- Back to sources link still present as fallback navigation
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-experience/03-06-SUMMARY.md`
</output>
