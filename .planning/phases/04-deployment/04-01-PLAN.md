---
phase: 04-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .dockerignore
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "A new developer can follow README and start the app with a single docker-compose up"
    - "The config reference table lists all supported columnMappings roles with types and descriptions"
    - "The README embeds the docker-compose.yml snippet verbatim in Quick Start so no file hunting is needed"
    - "Architecture overview is 2-3 bullet points only — no diagram"
    - "Troubleshooting tips address at least 4 common failure modes"
  artifacts:
    - path: "README.md"
      provides: "Full user-facing documentation"
      contains: "## Quick Start"
    - path: ".dockerignore"
      provides: "Reduce Docker build context"
      contains: "node_modules"
  key_links:
    - from: "README.md Quick Start"
      to: "docker-compose.yml snippet"
      via: "verbatim copy of docker-compose.yml services block"
    - from: "README.md Configuration Reference"
      to: "config/sources.example.json fields"
      via: "markdown table derived from example file"
---

<objective>
Write README.md for the repo root and add .dockerignore to reduce Docker build context.

Purpose: README is the first thing a new user reads — it must guide them from zero to running app, provide a complete config reference, and explain the architecture briefly.
Output: README.md (setup-first docs) + .dockerignore (build tooling hygiene)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-deployment/04-CONTEXT.md
@config/sources.example.json
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md — setup-first, config reference table, architecture brief, troubleshooting</name>
  <files>README.md</files>

  <read_first>
    - config/sources.example.json — all supported columnMappings fields and structure
    - docker-compose.yml — exact snippet to embed verbatim in Quick Start
    - .env.example — exact content to show in setup section
    - .planning/phases/04-deployment/04-CONTEXT.md — locked decisions D-01 through D-05
    - CLAUDE.md — project description, tech stack, constraints (source of truth for feature copy)
  </read_first>

  <action>
Create README.md at repo root. Use the Write tool to create the entire file.

The file must have these sections in this exact order (per D-01, D-05):
1. Project tagline (H1 + blockquote)
2. ## Features
3. ## Prerequisites
4. ## Quick Start
5. ## Configuration Reference
6. ## Architecture
7. ## Troubleshooting

Section content:

**H1 + tagline:**
  "# vizu-notion-local"
  "> Render Notion databases as interactive metro maps and process flow diagrams — config-driven, self-hosted, no code changes required."

**## Features** (4 bullet points):
  - "**Metro map visualization** — Hierarchical Notion databases (goals → projects → tasks) rendered as metro-line style diagrams using Metroviz"
  - "**Process flow diagrams** — Sequential/workflow databases rendered as node-edge flow charts using Vue Flow"
  - "**Config-driven** — Define sources and column mappings in a JSON file; restart container to apply"
  - "**Fully local** — Single `docker-compose up`, your Notion token stays server-side, no cloud relay"

**## Prerequisites** (3 bullets):
  - "Docker and Docker Compose installed"
  - "A Notion integration token ([create one here](https://www.notion.so/my-integrations))"
  - "Notion database IDs for the databases you want to visualize"

**## Quick Start** (3 numbered steps + docker-compose snippet):
  Step 1: Copy .env.example to .env and set NOTION_API_TOKEN
  Step 2: Copy config/sources.example.json to config/sources.json and fill in database IDs
  Step 3: Run docker-compose up, open http://localhost:3000

  After step 3, include a "docker-compose.yml (for reference)" section with a yaml fenced code block containing the EXACT content of docker-compose.yml (read it first). This is the content to embed:

    version: '3.9'

    services:
      app:
        build:
          context: .
          dockerfile: Dockerfile
        ports:
          - "3000:3000"
        volumes:
          - ./config:/app/config:ro
        environment:
          NODE_ENV: production
          NOTION_API_TOKEN: ${NOTION_API_TOKEN}
        env_file:
          - .env
        restart: unless-stopped

**## Configuration Reference** (3 sub-sections):

  Sub-section "Config file: `config/sources.json`":
  - One-liner: "Mount path: /app/config/sources.json"

  Sub-section "### Source object fields" (markdown table, 4 cols: Field | Type | Required | Description):
    | `databaseId` | string | Yes | Notion database ID (32-char hex from the database URL) |
    | `name`       | string | Yes | Display name shown in the UI source switcher |
    | `columnMappings` | object | Yes | Maps role names to Notion property/column names |

  Sub-section "### columnMappings roles" (markdown table, 4 cols: Role | Type | Viz Type | Description):
    | `title`       | string | Both  | Maps to the primary Notion title property (e.g. "Name") |
    | `status`      | string | Both  | Maps to a Notion Select or Status property for filtering |
    | `parent`      | string | Metro | Maps to a Notion Relation property pointing to parent entries — enables metro map hierarchy |
    | `children`    | string | Metro | Maps to a Notion Relation property pointing to child entries |
    | `relatedGoal` | string | Metro | Maps to any cross-database Relation property to link entries |
    | `assignee`    | string | Flow  | Maps to a People property |
    | `sequence`    | string | Flow  | Maps to a Number property for ordering flow nodes |

  After the table, add a bold note:
  "**Visualization type auto-detection:**
  - A source with `parent` or `children` in columnMappings is eligible for metro map
  - A source with `sequence` in columnMappings is eligible for process flow"

  Sub-section "### Example sources.json":
  Embed a json fenced code block with the EXACT content of config/sources.example.json (read it first).

**## Architecture** (3 bullet points only, per D-04):
  - "**Nuxt 3 server routes** proxy all Notion API calls — your integration token is never exposed to the browser"
  - "**LRU memory cache** (1-hour TTL) reduces redundant API calls and respects Notion's 3 req/s rate limit"
  - "**Config-driven** — no application code changes needed; only `config/sources.json` and `.env`"

**## Troubleshooting** (4 failure cases):
  Case 1: 'Container exits immediately / "Invalid config" error'
    - Ensure config/sources.json exists (copy from config/sources.example.json)
    - Verify all databaseId values are 32-character hex strings (remove hyphens from Notion URL ID)
    - Check that each property name in columnMappings matches the exact column name in your Notion database

  Case 2: '"NOTION_API_TOKEN not set" error'
    - Ensure .env exists and contains NOTION_API_TOKEN=secret_...
    - Verify your Notion integration has been shared with the target databases (database → Share → invite integration)

  Case 3: 'Blank diagram / no data loads'
    - Check browser console for API errors
    - Use the "Fetch All" button on the dashboard to trigger a fresh Notion API fetch
    - Confirm the Notion integration has read access to all configured databases

  Case 4: 'Port 3000 already in use'
    - Change the host port in docker-compose.yml: "3001:3000" and visit http://localhost:3001

NO screenshots, NO images, NO image tags (per D-03).
NO diagram in architecture section (per D-04).
  </action>

  <verify>
    <automated>grep -n "## Quick Start" README.md && grep -n "## Configuration Reference" README.md && grep -n "## Architecture" README.md && grep -n "## Troubleshooting" README.md && grep -n "columnMappings roles" README.md</automated>
  </verify>

  <acceptance_criteria>
    - README.md contains "## Quick Start"
    - README.md contains "## Configuration Reference"
    - README.md contains "columnMappings roles"
    - README.md contains "| \`parent\`" in a table row (the metro viz role)
    - README.md contains "./config:/app/config:ro" (verbatim from docker-compose.yml, per D-01)
    - README.md contains "## Architecture"
    - README.md contains "## Troubleshooting"
    - README.md contains "NOTION_API_TOKEN" in the Quick Start section
    - grep -c "!\[" README.md returns 0 (no image tags, per D-03)
  </acceptance_criteria>

  <done>README.md exists at repo root with all 7 sections, config reference table with all 7 columnMappings roles, verbatim docker-compose snippet, 3-bullet architecture overview, and troubleshooting for 4 failure cases. No screenshots.</done>
</task>

<task type="auto">
  <name>Task 2: Create .dockerignore — reduce build context</name>
  <files>.dockerignore</files>

  <read_first>
    - Dockerfile — confirms builder stage runs COPY . . so we need to exclude dev/artifact directories
    - .planning/phases/04-deployment/04-CONTEXT.md — discretion note about .dockerignore
  </read_first>

  <action>
Create .dockerignore at repo root with exactly these lines:

    node_modules
    .output
    .nuxt
    .git
    .gitignore
    *.log
    .env
    .env.local
    .env.*.local
    .planning
    docs
    dist
    coverage
    .vscode
    .idea
    *.test.ts
    *.spec.ts

Do NOT add lines excluding:
  - config/ (sources.example.json needed in build context)
  - vendor/ (Metroviz vendored modules needed by build)
  - public/ (static assets)
  - server/ (application code)
  - components/, pages/, composables/, layouts/ (app source)
  - nuxt.config.ts, package.json, package-lock.json, tsconfig.json (build configuration)

Note: Do NOT add "*.md" as a global exclusion — CLAUDE.md and other md files may be needed by the build or cause issues if excluded globally. Exclude by directory instead (.planning).
  </action>

  <verify>
    <automated>grep "node_modules" .dockerignore && grep ".planning" .dockerignore && grep "^\.git$" .dockerignore</automated>
  </verify>

  <acceptance_criteria>
    - .dockerignore exists at repo root
    - .dockerignore contains "node_modules"
    - .dockerignore contains ".planning"
    - .dockerignore contains ".git" as a line
    - .dockerignore contains ".output"
    - .dockerignore contains ".nuxt"
    - grep "^config$" .dockerignore returns empty (config/ is NOT excluded)
  </acceptance_criteria>

  <done>.dockerignore exists at repo root. Docker build context excludes node_modules, .git, .output, .nuxt, .planning, docs, and dev tooling files. config/ and vendor/ remain included.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
grep -c "^##" README.md
# Expect: 6+ sections

grep -c "!\[" README.md
# Expect: 0 (no screenshots)

grep -E "parent|children|sequence|assignee|relatedGoal" README.md | grep "|"
# Expect: 5 matching table rows

cat .dockerignore
# Expect: node_modules, .git, .output, .nuxt, .planning present; no "config" line
```
</verification>

<success_criteria>
README.md covers setup-first flow, config reference table with all 7 columnMappings roles, verbatim docker-compose snippet, 3-bullet architecture overview, and troubleshooting for 4 failure cases. .dockerignore excludes dev/build artifacts without blocking app source or vendored modules.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment/04-01-SUMMARY.md`
</output>
