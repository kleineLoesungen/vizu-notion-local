---
phase: 06-mermaid-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/FilterPanel.vue
autonomous: true
requirements:
  - MERM-FILTER-HEIGHT
must_haves:
  truths:
    - "The node visibility list in the filter panel is no longer capped at 20rem — it expands to show all nodes without inner scrolling"
    - "The outer filter panel never exceeds the viewport height and provides its own scroll when the list is long"
  artifacts:
    - path: "components/FilterPanel.vue"
      provides: "Removed max-height inline styles from both list containers; added max-h-screen to outer panel"
      contains: "max-h-screen"
  key_links:
    - from: "outer panel div"
      to: "node list divs"
      via: "outer panel handles overflow; inner lists have no height cap"
      pattern: "max-h-screen"
---

<objective>
Remove the 20rem max-height cap on the FilterPanel's inner node list containers so the list fills its available space. Add `max-h-screen overflow-y-auto` to the outer panel div so it never overflows the viewport (D-06).

Purpose: The filter panel currently cuts off after ~12 nodes, requiring inner scrolling. After this fix, the panel itself scrolls when needed but never clips the node list.

Output: Two inline `style="max-height: 20rem;"` attributes removed from `FilterPanel.vue`; `max-h-screen` added to the outer panel div's class list.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Exact lines to change in FilterPanel.vue -->

Line 4 (outer panel div — ADD max-h-screen):
```html
<div
  v-if="open"
  class="w-72 p-4 bg-gray-50 border-l border-gray-200 overflow-y-auto flex-shrink-0"
```
→ Change class to add `max-h-screen`:
```html
  class="w-72 p-4 bg-gray-50 border-l border-gray-200 overflow-y-auto flex-shrink-0 max-h-screen"
```

Line 82 (grouped list — REMOVE inline style):
```html
<div v-if="hasParentGroups" class="space-y-2 overflow-y-auto" style="max-height: 20rem;">
```
→ Remove `style="max-height: 20rem;"`:
```html
<div v-if="hasParentGroups" class="space-y-2">
```
Also remove `overflow-y-auto` from the inner div — the outer panel handles scrolling.

Line 116 (flat list — REMOVE inline style):
```html
<div v-else class="space-y-1 overflow-y-auto" style="max-height: 20rem;">
```
→ Remove `style="max-height: 20rem;"`:
```html
<div v-else class="space-y-1">
```
Also remove `overflow-y-auto` from the inner div.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove max-height caps and fix outer panel height</name>
  <files>components/FilterPanel.vue</files>
  <read_first>components/FilterPanel.vue</read_first>
  <action>
Make exactly three targeted changes to `components/FilterPanel.vue`:

**Change 1 — Outer panel div (line ~4):**
Add `max-h-screen` to the class list. This caps the panel at 100vh so it never pushes page content down. The existing `overflow-y-auto` on this div then handles scrolling when content is taller than the viewport.

Before:
```html
class="w-72 p-4 bg-gray-50 border-l border-gray-200 overflow-y-auto flex-shrink-0"
```
After:
```html
class="w-72 p-4 bg-gray-50 border-l border-gray-200 overflow-y-auto flex-shrink-0 max-h-screen"
```

**Change 2 — Grouped node list div (line ~82):**
Remove both `overflow-y-auto` and `style="max-height: 20rem;"` from the grouped list container. The outer panel now owns scrolling.

Before:
```html
<div v-if="hasParentGroups" class="space-y-2 overflow-y-auto" style="max-height: 20rem;">
```
After:
```html
<div v-if="hasParentGroups" class="space-y-2">
```

**Change 3 — Flat node list div (line ~116):**
Remove both `overflow-y-auto` and `style="max-height: 20rem;"` from the flat list container.

Before:
```html
<div v-else class="space-y-1 overflow-y-auto" style="max-height: 20rem;">
```
After:
```html
<div v-else class="space-y-1">
```

No other changes. The rest of FilterPanel.vue is untouched.
  </action>
  <verify>
    <automated>grep -n "max-height" /Users/sebastianwiller/Documents/github/vizu-notion-local/components/FilterPanel.vue && echo "FAIL: max-height still present" || echo "PASS: max-height removed"
grep -n "max-h-screen" /Users/sebastianwiller/Documents/github/vizu-notion-local/components/FilterPanel.vue</automated>
  </verify>
  <acceptance_criteria>
- `grep "max-height" components/FilterPanel.vue` returns no matches (both inline styles removed)
- `grep "max-h-screen" components/FilterPanel.vue` returns exactly 1 match (outer panel div)
- `grep "space-y-2" components/FilterPanel.vue` still returns a match (grouped list div preserved, just without the height cap)
- `grep "space-y-1" components/FilterPanel.vue` still returns a match (flat list div preserved)
  </acceptance_criteria>
  <done>
Both inline `style="max-height: 20rem;"` attributes are removed from FilterPanel.vue. The outer panel div has `max-h-screen` in its class list. The inner list divs no longer have `overflow-y-auto` (outer panel handles scroll).
  </done>
</task>

</tasks>

<verification>
After implementation:
1. `grep "max-height" components/FilterPanel.vue` — must return empty
2. `grep "max-h-screen" components/FilterPanel.vue` — must return 1 match on the outer panel div
3. Open the app in browser with a source that has many nodes → filter panel should show all nodes without inner scroll
</verification>

<success_criteria>
- Zero `style="max-height: ..."` attributes remaining in FilterPanel.vue
- Outer panel div has `max-h-screen` so panel height is viewport-bounded
- Panel scrolls as a whole (not inner list scrolling independently)
</success_criteria>

<output>
After completion, create `.planning/phases/06-mermaid-improvements/06-02-SUMMARY.md`
</output>
