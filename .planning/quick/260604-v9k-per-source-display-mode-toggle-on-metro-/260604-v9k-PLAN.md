---
id: 260604-v9k
type: quick
autonomous: true
files_modified:
  - pages/visualizations/[sourceId].vue
---

<objective>
Add a per-source display mode toggle for additional sources on the metro map. Sources with both `date` and `next` roles can be rendered either as a full metro line (existing behavior) or as a flat milestone overlay (next stripped from columnMappings). Sources with only `date` default to milestones with no toggle shown.

Purpose: Users can choose whether a linked roadmap/project source appears as a sequenced line or as a simple milestone scatter, reducing visual noise when sequencing of the extra source is irrelevant.
Output: Updated [sourceId].vue with reactive `sourceDisplayModes` state, mode-aware `metrovizData` computed, and inline segmented toggle in the Additional Sources bar.
</objective>

<context>
@pages/visualizations/[sourceId].vue
@composables/useMetrovizData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sourceDisplayModes state + watcher + mode-aware metrovizData</name>
  <files>pages/visualizations/[sourceId].vue</files>
  <action>
**Script changes only — no template changes in this task.**

1. After the `selectedSourceIds` ref declaration (line ~357), add:

```ts
// Per-source display mode: 'line' (full next-graph) or 'milestones' (date-only overlay)
const sourceDisplayModes = reactive<Record<string, 'milestones' | 'line'>>({})
```

Import `reactive` if not already in the `{ ref, computed, watch, ... }` import list (it is not currently imported — add it).

2. After the `watch(sourceId, ...)` that resets `selectedSourceIds` (around line 359), add a new watcher:

```ts
// Initialize display mode for newly eligible sources; never overwrite an existing choice.
watch(eligibleAdditionalSources, (sources) => {
  for (const src of sources) {
    if (!(src.id in sourceDisplayModes)) {
      sourceDisplayModes[src.id] = 'next' in (src.columnMappings ?? {}) ? 'line' : 'milestones'
    }
  }
}, { immediate: true })
```

3. In the `metrovizData` computed (currently ~line 572), inside the `.map(d => { ... })` that builds `extrasData`, replace the `useMetrovizData(...)` call with a mode-aware version:

**Find** (in the extrasData map):
```ts
return useMetrovizData(visiblePages, d.source.columnMappings, d.source.name)
```

**Replace with:**
```ts
const mode = sourceDisplayModes[d.source.id] ?? ('next' in (d.source.columnMappings ?? {}) ? 'line' : 'milestones')
const effectiveMappings = mode === 'milestones'
  ? Object.fromEntries(Object.entries(d.source.columnMappings).filter(([k]) => k !== 'next'))
  : d.source.columnMappings
return useMetrovizData(visiblePages, effectiveMappings, d.source.name)
```

No other changes to the computed.
  </action>
  <verify>
    `npx nuxi typecheck` passes (or `grep -n "reactive" pages/visualizations/\[sourceId\].vue` confirms import added).
    Manually: load the page and confirm no TypeScript/console errors.
  </verify>
  <done>
    `sourceDisplayModes` reactive object declared and imported, watcher initializes entries on eligible source change, `metrovizData` computed strips `next` from columnMappings when mode is `'milestones'`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add segmented toggle to Additional Sources row in template</name>
  <files>pages/visualizations/[sourceId].vue</files>
  <action>
In the template, find the "Multi-source selector" `v-for` block (currently lines ~181-194):

```html
<label
  v-for="src in eligibleAdditionalSources"
  :key="src.id"
  class="flex items-center gap-1.5 cursor-pointer select-none"
>
  <input
    type="checkbox"
    :checked="selectedSourceIds.has(src.id)"
    class="w-3.5 h-3.5 cursor-pointer"
    @change="toggleSourceSelection(src.id)"
  />
  <span class="text-xs text-gray-700">{{ src.name }}</span>
</label>
```

Replace with:

```html
<div
  v-for="src in eligibleAdditionalSources"
  :key="src.id"
  class="flex items-center gap-2"
>
  <label class="flex items-center gap-1.5 cursor-pointer select-none">
    <input
      type="checkbox"
      :checked="selectedSourceIds.has(src.id)"
      class="w-3.5 h-3.5 cursor-pointer"
      @change="toggleSourceSelection(src.id)"
    />
    <span class="text-xs text-gray-700">{{ src.name }}</span>
  </label>

  <!-- Mode toggle: only shown when checked AND source has a 'next' role -->
  <template v-if="selectedSourceIds.has(src.id) && 'next' in (src.columnMappings ?? {})">
    <div class="flex rounded overflow-hidden border border-gray-200 text-[10px]">
      <button
        type="button"
        class="px-1.5 py-0.5"
        :class="sourceDisplayModes[src.id] === 'milestones' ? 'bg-blue-600 text-white' : 'bg-white text-gray-500 hover:bg-gray-50'"
        @click="sourceDisplayModes[src.id] = 'milestones'"
      >Milestones</button>
      <button
        type="button"
        class="px-1.5 py-0.5 border-l border-gray-200"
        :class="sourceDisplayModes[src.id] === 'line' ? 'bg-blue-600 text-white' : 'bg-white text-gray-500 hover:bg-gray-50'"
        @click="sourceDisplayModes[src.id] = 'line'"
      >Line</button>
    </div>
  </template>
</div>
```

Key decisions per spec:
- Toggle only visible when the source checkbox is checked (`selectedSourceIds.has(src.id)`)
- Toggle only rendered when source has `'next' in columnMappings` (no toggle for date-only sources)
- Direct property assignment `sourceDisplayModes[src.id] = '...'` works because `sourceDisplayModes` is `reactive({})` — Vue tracks property writes on reactive objects
  </action>
  <verify>
    Visual check: check an additional source with `next` in its columnMappings → toggle "Milestones / Line" appears inline. Switch to Milestones → map re-renders showing a flat overlay (single zone, no sequenced lines). Switch back to Line → full line rendering returns. Sources without `next` show no toggle.
  </verify>
  <done>
    Segmented toggle renders inline next to each checked additional source that has a `next` role. Switching modes updates `sourceDisplayModes[src.id]` reactively, which triggers `metrovizData` recomputation with the appropriate columnMappings.
  </done>
</task>

</tasks>

<success_criteria>
- Additional sources with `next` in their columnMappings show a "Milestones / Line" toggle when checked
- Milestones mode passes columnMappings without the `next` key to `useMetrovizData`, producing the parent-based flat grouping path (single zone of milestone stations)
- Line mode passes full columnMappings (existing behavior unchanged)
- Sources with only `date` (no `next`) default silently to milestones with no toggle shown
- No TypeScript errors introduced; `reactive` correctly imported
</success_criteria>

<output>
No SUMMARY.md required for quick tasks. Update STATE.md quick tasks table after commit.
</output>
